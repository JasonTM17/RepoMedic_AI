# Contributing

Use Node 22+ and npm. Run formatting, linting, type checks, and relevant tests before committing.

Keep commits focused and conventional. Never commit `.env` files, credentials, `.claude/`, `plans/`, private notes, generated local run artifacts, or AI co-author trailers.
